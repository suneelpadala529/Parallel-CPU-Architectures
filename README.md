# Parallel-CPU-Architectures using OPEN-MPI
Implementation of Parallel CPU architectures (linear ring , mesh and Hyper cube architectures)  and simulated Merge sort and binary search on these architectures.
